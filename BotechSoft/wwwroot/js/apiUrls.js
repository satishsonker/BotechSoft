const baseUrl = location.origin;
//const baseUrl = "http://letspaint.somee.com";
const url = {
    root: {
        home: {},
        query: {
            sendQuery:'/query/sendQuery'
        }
    }
}